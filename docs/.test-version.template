<!--
  This documentation is inserted in release note for each release.
  All variables are defined in `hack/test-env.sh`.
-->

The following Gateway API version and Ingress were tested as part of the release.

### Tested Gateway API version

| Tested Gateway API       |
| ------------------------ |
| "${GATEWAY_API_VERSION}" |

### Tested Ingress

| Ingress | Tested version          | Unavailable features           |
| ------- | ----------------------- | ------------------------------ |
| Istio   | v"${ISTIO_VERSION}"     | "${ISTIO_UNSUPPORTED_E2E_TESTS}"   |
| Contour | "${CONTOUR_VERSION}"    | "${CONTOUR_UNSUPPORTED_E2E_TESTS}" |
| Envoy Gateway | "${ENVOY_GATEWAY_VERSION}"    | "${ENVOY_GATEWAY_UNSUPPORTED_E2E_TESTS}" |